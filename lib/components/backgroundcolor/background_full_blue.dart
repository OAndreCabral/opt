import 'package:flutter/material.dart';

class BackgroundFullBlue extends StatelessWidget {
  const BackgroundFullBlue({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: const Color.fromRGBO(23, 49, 84, 1),);
  }
}
